<Window x:Class="CryptoLab.Transaction"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CryptoLab"
        mc:Ignorable="d"
        Title="Transaction" Height="200" Width="400"
        ResizeMode="NoResize">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="5*"/>
        </Grid.RowDefinitions>
        <GroupBox x:Name="groupBox" Header="Parameters" Grid.ColumnSpan="2">
            <StackPanel>
                <Label x:Name="label1" Content="Reciver" Target="{Binding ElementName=textBox1}"/>
                <TextBox x:Name="textBox1" />
                <Label x:Name="label2" Content="Total" Target="{Binding ElementName=textBox2}"/>
                <TextBox x:Name="textBox2"/>
            </StackPanel>
        </GroupBox>
        <Button x:Name="SendButton" Content="Send" Margin="10px"  Grid.Row="1" Click="SendButton_Click"/>
        <Button x:Name="BackButton" Content="Back" Margin="10px" Grid.Row="1"  Click="BackButton_Click" Grid.Column="1" />
    </Grid>
</Window>
